{
  "name": "<domain>-panel",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "esbuild src/<name>.ts --bundle --minify --format=esm --target=es2021 --outfile=../custom_components/<domain>/panel/<name>.js",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "lit": "^3.2.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
